# Skill 使用规则

技能文件位于项目根目录的 `Skills/` 文件夹，系统在每次请求时自动扫描该目录生成可用技能清单（见下方"当前可用技能文件"）。

匹配到需求时，必须先用清单中给出的 `read_file(path="...")` 读取对应技能的完整内容，再按技能正文执行；不要只凭技能名称或描述猜测流程。

新增、删除、重命名 `Skills/` 下的 `.md` 文件后无需手动维护本文件，下一次 AI 请求会自动使用最新清单。
